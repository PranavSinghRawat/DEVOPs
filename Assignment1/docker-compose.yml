version: '3'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=sqlite:///data.db

  db:
    image: postgres
    volumes:
      - ./data.db:/var/lib/postgresql/data
